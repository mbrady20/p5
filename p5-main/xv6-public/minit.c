#include "types.h"
#include "stat.h"
#include "user.h"

int main(int argc, char *argv[])
{
    if (argc < 2)
    {
        printf(2, "Usage: minit <lock>\n");
        exit();
    }

    // m = (struct mutex *)malloc(sizeof(struct mutex));
    exit();
}